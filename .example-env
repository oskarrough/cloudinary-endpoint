# Environment config

# rename this file to `.env` and store your secrets and config variables in here
# reference these in your code with process.env.SECRET
# note: .env is a shell file so there can't be spaces around '=

API_KEY=123
API_SECRET=123abc
CLOUD_NAME=helloworld
